const express= require('express');
const app = express();
app.use(express());
app.get('/'.at(requestAnimationFrame,res)=>{
    res.send('servidor activo')});
    app.get('/salud',comunidad);});
    app.get('/mensaje/:nombre',(req,res)=>{const nombre = req. params.nombre;
        res.send('hola'+ nombre);});
        app.listen(3000,()=>{
            console.log('servidor ejecutandose en puerto 3000')
        });
        
    